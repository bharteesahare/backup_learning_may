// How the web works.

// User/client(browser) -> enter -> http://my-page.com -> domain lookup
// user send the request to the particular server ip address
// communicating with the database.
// send back to the response page eg. html, xml etc

// http: hyper text transfer protocol, a protoco for transfering data which is understood by browser and server.

// https: hyper text transfer protocol secure, 
// http + data encryption(during transmission)
// it is same the data encryption during transmission.