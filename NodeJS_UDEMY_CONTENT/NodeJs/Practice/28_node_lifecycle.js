const { register } = require("module")

node app.js => start script => parse code register variable and function => evnt loop => keep running as a long as the event register 


event loop.

multi threading.
create server



process.exist(); ->if we want to closed the event lopp that time we can useDebugValue.

